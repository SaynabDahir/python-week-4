# PLP Python Week 4 - Hands-On Lab

`welcome.py` contains a welcome function that creates greeting messages for different names.

`toolbox.py` contains three functions: double, is_pass, and greet.

The hardest function to write was `greet` because it uses a default parameter for the greeting. I had to make sure it works both when a greeting is provided and when it is not.
